export const City = (cityObj) => {
    return (
        `<h2>Cities</h2>
        <ul>
            <li>${cityObj.city1}</li>
            <li>${cityObj.city2}</li>
            <li>${cityObj.city3}</li>
        </ul>
        `
    )
}